import React from 'react';

import Logo from '../Logo';

import './Startup.scss';

function Startup() {

    return (
        <div className="startup">
            <Logo />
        </div>
    );
};

export default Startup;